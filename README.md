# network_measuring_tool
Network measuring tool. Measures the end-to-end throughput of the network, jitter and an estimation of the one-way latency. The tool works in a client-server model. The server waits for connections and the clients connect to the remote server to begin the experiment.
